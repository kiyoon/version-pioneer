pytest
pytest-coverage

# these are intentionally put in dev requirements because users only use one of the backends,
# and they will already have put it in their requirements anyway.
setuptools
pdm-backend
hatchling
